mode: ContinuousDeployment
source-branches: ['main', 'feature/*', 'bug/*']
branches:
  main:
    increment: Patch
  feature:
    regex: ^feature[\/-](?<StoryNumber>\d{6,7})-.*
    label: '{StoryNumber}'
    increment: Minor
  bug:
    regex: ^bug[\/-](?<StoryNumber>\d{6,7})-.*
    label: '{StoryNumber}'
    increment: Patch
